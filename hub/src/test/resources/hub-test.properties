sender.id=${sender.id}
sftp.user=${sftp.user}
sftp.host=${sftp.host}
sftp.path=${sftp.path}
sftp.password=${sftp.password}
sftp.key.file=${sftp.key.file}
sftp.key.password=${sftp.key.password}

smtp.port=${smtp.port}